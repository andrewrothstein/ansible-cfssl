---
galaxy_info:
  author: Andrew Rothstein
  description: installs cfssl
  company: BlackRock
  license:
    - MIT
  min_ansible_version: 2.0
  platforms:
    - name: EL
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
    - name: MacOSX
      versions:
        - all

  galaxy_tags:
    - cfssl
    - tls
    - pki
    
dependencies:
  - role: andrewrothstein.go-dev
    version: v2.0.3
    go_dev_escalate: '{{ cfssl_privilege_escalation }}'
    go_dev_get: '{{ cfssl_go_dev_get }}'
